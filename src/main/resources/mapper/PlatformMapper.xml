<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dao.PlatformDao">

    <update id="insertScore">
        update student set companyscore=#{companyscore} where id=#{id} and isdelete = 0
    </update>
    <select id="findCompanyName" resultType="java.lang.String" parameterType="integer">
        select company from company where id = #{value}
    </select>
    <select id="showStudent" parameterType="string" resultType="com.pojo.Student">
        select id,stuname,companyscore from student where company = #{value}
    </select>
</mapper>